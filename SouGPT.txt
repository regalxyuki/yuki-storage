case 'testsewa': {
    if (!isCreator) return Reply('❌ Khusus owner');
    
    // Simulasi order paid
    const testOrder = {
        id: 'YU-TEST-' + Date.now(),
        nama: 'Test User',
        nomor: '6288705666357',
        groupLink: 'https://chat.whatsapp.com/ClZxyUdMt4GA7hfJ6HfUaC',
        inviteCode: 'xxxx',
        duration: 1,
        status: 'paid',
        depositId: 'TEST-' + Date.now(),
        processedByBot: false
    };
    
    // Simpan ke memory DB (ini cuma simulasi, aslinya dari website)
    // ... atau langsung test join grup:
    
    try {
        const groupRes = await alip.groupAcceptInvite(testOrder.inviteCode);
        Reply(`✅ Bot berhasil join grup!\nID: ${groupRes.id || groupRes}`);
    } catch (e) {
        Reply(`❌ Gagal join: ${e.message}`);
    }
}
break;
